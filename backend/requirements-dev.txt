# Test-only dependencies — not installed in the Docker image (Dockerfile installs
# requirements.txt only), so the deployed API doesn't ship test tooling.
-r requirements.txt

pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
